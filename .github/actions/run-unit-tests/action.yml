name: run-unit-tests
description: "Run unit tests."
runs:
  using: "composite"
  steps:
    - name: setup-environment
      uses: ./.github/actions/setup-environment

    - name: run-pytest
      run: poetry run pytest -m "unit" tests
      shell: bash
