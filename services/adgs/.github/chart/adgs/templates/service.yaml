apiVersion: v1
kind: Service
metadata:
  name: {{ include "adgs.fullname" . }}
  labels:
    {{- include "adgs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adgs.selectorLabels" . | nindent 4 }}
